diff --git a/src/client/voice/WebRtcWrapper.ts b/src/client/voice/WebRtcWrapper.ts
index 997d78ea86793dea2ac11532aa0a8c85efe2197b..28067bbb5146e37a6347b1e33820f3d1c097bcfe 100644
--- a/src/client/voice/WebRtcWrapper.ts
+++ b/src/client/voice/WebRtcWrapper.ts
@@ -168,9 +168,14 @@ export class WebRtcConnWrapper {
       CodecPayloadType.opus.payload_type,
       CodecPayloadType.opus.clockRate,
     );
+    // Patched: set playout delay to 0/0 on both audio and video (matching
+    // WrappedStream). The original values (audio max=1=100ms, video max=10=1s)
+    // tell Discord's client it can buffer video for up to 1 second while playing
+    // audio almost immediately, causing a ~1s audio-ahead-of-video offset.
+    // Zero playout delay forces immediate rendering of both streams.
     rtpConfigAudio.playoutDelayId = 5;
     rtpConfigAudio.playoutDelayMin = 0;
-    rtpConfigAudio.playoutDelayMax = 1;
+    rtpConfigAudio.playoutDelayMax = 0;
     this._audioPacketizer = new RtpPacketizer(rtpConfigAudio);
     this._audioPacketizer.addToChain(new RtcpSrReporter(rtpConfigAudio));
     this._audioPacketizer.addToChain(new RtcpNackResponder());
@@ -184,7 +189,7 @@ export class WebRtcConnWrapper {
     );
     rtpConfigVideo.playoutDelayId = 5;
     rtpConfigVideo.playoutDelayMin = 0;
-    rtpConfigVideo.playoutDelayMax = 10;
+    rtpConfigVideo.playoutDelayMax = 0;
     switch (this._videoCodec) {
       case "H264":
         this._videoPacketizer = new H264RtpPacketizer(
diff --git a/src/media/BaseMediaStream.ts b/src/media/BaseMediaStream.ts
index 6a53ad1490979bbb782599efca0eaf5a640a5d28..e4477e508b36b7eeb4326573c7355b8d08ac0ae3 100644
--- a/src/media/BaseMediaStream.ts
+++ b/src/media/BaseMediaStream.ts
@@ -5,7 +5,7 @@ import type { Packet } from "node-av";
 
 export class BaseMediaStream extends Writable {
   private _pts?: number;
-  private _syncTolerance = 20;
+  private _syncTolerance = 50;
   private _loggerSend: Log;
   private _loggerSync: Log;
   private _loggerSleep: Log;
@@ -87,6 +87,27 @@ export class BaseMediaStream extends Writable {
   private resetTimingCompensation() {
     this._startTime = this._startPts = undefined;
   }
+
+  /**
+   * Precision sleep: setTimeout for the bulk (waking ~2ms early),
+   * then spin-wait for the remainder. This overcomes the Windows
+   * timer resolution of ~15.6ms which causes visible stutter with
+   * standard setTimeout alone.
+   * Based on WrappedStream's implementation.
+   */
+  private async precisionSleep(ms: number): Promise<void> {
+    if (ms < 1) return;
+    const deadline = performance.now() + ms;
+    if (ms > 5) {
+      // Sleep the coarse portion, waking ~2ms early to spin the rest
+      await setTimeout(Math.max(1, Math.floor(ms - 2)));
+    }
+    // Spin-wait for sub-millisecond accuracy
+    while (performance.now() < deadline) {
+      // Intentional busy-wait for precision
+    }
+  }
+
   async _write(
     frame: Packet,
     _: BufferEncoding,
@@ -153,7 +174,7 @@ export class BaseMediaStream extends Writable {
         },
         "Stream is behind. Not sleeping for this frame",
       );
-      this.resetTimingCompensation();
+      // Patched: do NOT resetTimingCompensation() — keeps timing stable
       callback(null);
     } else if (this.sync && this.isAhead()) {
       do {
@@ -167,9 +188,9 @@ export class BaseMediaStream extends Writable {
           },
           `Stream is ahead. Waiting for ${frametime}ms`,
         );
-        await setTimeout(frametime);
+        await this.precisionSleep(frametime);
       } while (this.sync && this.isAhead());
-      this.resetTimingCompensation();
+      // Patched: do NOT resetTimingCompensation() — keeps timing stable
       callback(null);
     } else {
       this._loggerSleep.debug(
@@ -184,7 +205,8 @@ export class BaseMediaStream extends Writable {
         },
         `Sleeping for ${sleep}ms`,
       );
-      setTimeout(sleep).then(() => callback(null));
+      // Use precisionSleep instead of setTimeout for accurate frame pacing
+      this.precisionSleep(sleep).then(() => callback(null));
     }
     frame.free();
   }
diff --git a/src/media/LibavDemuxer.ts b/src/media/LibavDemuxer.ts
index 01ebf724d0183fcdb92d918d21c3f73217f51c62..0093db72b3fadf33a2d70c0d924d7254c011123c 100644
--- a/src/media/LibavDemuxer.ts
+++ b/src/media/LibavDemuxer.ts
@@ -132,11 +132,11 @@ export async function demux(input: Readable, { format }: DemuxerOptions) {
   let aInfo: AudioStreamInfo | undefined;
   const vPipe = new PassThrough({
     objectMode: true,
-    writableHighWaterMark: 128,
+    writableHighWaterMark: 8,
   });
   const aPipe = new PassThrough({
     objectMode: true,
-    writableHighWaterMark: 128,
+    writableHighWaterMark: 8,
   });
 
   const vbsf: BitStreamFilterAPI[] = [];
